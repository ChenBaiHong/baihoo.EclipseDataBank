<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jaxws="http://cxf.apache.org/jaxws" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-3.0.xsd
    http://cxf.apache.org/jaxws 
    http://cxf.apache.org/schemas/jaxws.xsd">
    <!-- 引入CXF Bean定义如下，早期的版本使用 -->
	<!-- cxf3.x版本以后，导入如下文件会报错：cxf3.x版本，该文件不存在。 <import resource="classpath:META-INF/cxf/cxf-extension-soap.xml"/> -->
	<import resource="classpath:META-INF/cxf/cxf.xml" />
	<!-- cxf-servlet.xml 为空配置文件，可以不导入 -->
	<import resource="classpath:META-INF/cxf/cxf-servlet.xml" />
	<!-- 
	===========================配置類形式的webservice===========================
	address: tomcat 的 host 域名 == http://ip:port/projectName/service/配置的一端路径
	http://ip:port/projectName/service/userWs2
	 -->
	<jaxws:endpoint id="userService2WsServer" address="/userWs2"  implementor="com.baihoo.web.cxf.server2.UserService2">
		<!-- 打印输入消息的日志拦截器 -->
		<jaxws:inInterceptors>
			<bean class="org.apache.cxf.interceptor.LoggingInInterceptor"></bean>
		</jaxws:inInterceptors>
		<!-- 打印输出消息的日志拦截器 -->
		<jaxws:outInterceptors>
			<bean class="org.apache.cxf.interceptor.LoggingOutInterceptor"></bean>
		</jaxws:outInterceptors>
	</jaxws:endpoint>
	<!-- 
	===========================配置帶有接口類形式的webservice===========================
	address: tomcat 的 host 域名 == http://ip:port/projectName/service/配置的一端路径
	http://ip:port/projectName/service/userWs
	serviceClass：服務接口類
	 -->
	<jaxws:server id="userServiceWsServer" address="/userWs" serviceClass="com.baihoo.web.cxf.server.UserService">
		<!-- 設置實現接口類 -->
		<jaxws:serviceBean>
			<bean class="com.baihoo.web.cxf.server.impl.UserServiceImpl"></bean>
			<!-- <ref bean=""/> -->
		</jaxws:serviceBean>
		<!-- 打印输入消息的日志拦截器 -->
		<jaxws:inInterceptors>
			<bean class="org.apache.cxf.interceptor.LoggingInInterceptor"></bean>
		</jaxws:inInterceptors>
		<!-- 打印输出消息的日志拦截器 -->
		<jaxws:outInterceptors>
			<bean class="org.apache.cxf.interceptor.LoggingOutInterceptor"></bean>
		</jaxws:outInterceptors>
	</jaxws:server>
</beans>